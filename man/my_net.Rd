% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rr_network.R
\name{my_net}
\alias{my_net}
\title{Compute a weighted term-term similarity network (WIP)}
\usage{
my_net(
  x,
  gene,
  top = 50,
  value_cutoff = 0.05,
  value_type = "Padj",
  weight_cutoff = 0.2,
  sep = ","
)
}
\arguments{
\item{x}{A data frame of enrichment results containing columns: Term,
GeneID, Significant, and the column specified by \code{value_type}.}

\item{gene}{A DEG data frame (with \code{padj} column and gene row names)
or a character vector of gene identifiers.}

\item{top}{Integer. Maximum number of top terms to include. Default 50.}

\item{value_cutoff}{Numeric. P-value cutoff for filtering. Default 0.05.}

\item{value_type}{Character. Column name for significance values.
Default \code{"Padj"}.}

\item{weight_cutoff}{Numeric. Minimum weight threshold (currently unused
inside this function but reserved for downstream filtering). Default 0.2.}

\item{sep}{Character. Separator for gene IDs in the GeneID column.
Default \code{","}.}
}
\value{
A list with components: \code{w} (numeric matrix of pairwise
  weights), \code{value_col} (named numeric vector of -log10 values),
  and \code{x} (filtered enrichment data frame).
}
\description{
Custom implementation of a gene-overlap-based network for enrichment terms.
Computes pairwise term similarity weights based on shared genes and their
significance. Returns the weight matrix and filtered enrichment data.
}
\details{
Note: This function uses \code{my_} prefix as an informal convention
for work-in-progress internal helpers. It does not follow the \code{rr_}
prefix convention used by the main exported plot functions.
}
