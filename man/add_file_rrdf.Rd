% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/file_handling.R
\name{add_file_rrdf}
\alias{add_file_rrdf}
\title{Add Enrichment Result to File Table}
\usage{
add_file_rrdf(
  df,
  name,
  annot = "?",
  keytype = "?",
  ontology = "?",
  species = "?",
  file = FALSE
)
}
\arguments{
\item{df}{Existing file tracking dataframe (or NULL for new table)}

\item{name}{Name identifier for the enrichment result}

\item{annot}{Annotation type (GO, KEGG, Reactome, or '?' if unknown)}

\item{keytype}{Gene ID keytype used (or '?' if unknown)}

\item{ontology}{Ontology used (BP, MF, CC, or '?' if unknown)}

\item{species}{Species analyzed (or '?' if unknown)}

\item{file}{Logical: TRUE if from file upload, FALSE if from richStudio enrichment}
}
\value{
Updated file tracking dataframe with columns: name, from_deg, annot, keytype, ontology, species
  Appends "_enriched" to name if file=FALSE
}
\description{
Adds an enrichment result to the master file tracking table.
Can handle both file-uploaded results and results from richStudio enrichment analysis.
}
\examples{
\dontrun{
file_table <- add_file_rrdf(NULL, "go1", annot="GO", keytype="SYMBOL",
                            ontology="BP", species="human", file=TRUE)
}

}
